# Session: 64784803-cfae-416b-98fe-ca61efc0882b

## Date: 2026-05-25

## Work Completed

### 1. Now Playing Info Persistence (ROLLED BACK)
- Changed visibility condition in `MusicVisualisation.xml` to keep music info panel visible permanently
- Reverted as part of queue rollback

### 2. Queue Move Buttons (ROLLED BACK)
- Added up/down arrows to playlist views (50, 51, 53) for single-view song reordering
- Fully implemented then explicitly rolled back to stock per user request

### 3. Remove piers/nexus from deploy.sh (COMPLETED)
- Updated `deploy.sh` to only build for omega after user removed piers/nexus folders
- Removed all dual-version logic, consolidated to omega-only paths

### 4. M3U Playlist Path Fixer Addon (COMPLETED)
- Created `omega/service.playlist.fixer/` — a Kodi service addon
- **addon.xml**: `xbmc.service` extension, `start="login"`, minimal metadata
- **service.py**: Polls every 60s, rewrites `Music/` → `/media/mmcblk1p1-mmc-SD_0x00000681-pa/files/` in `.m3u` files
- Source: `/media/mmcblk1p1-mmc-SD_0x00000681-pa/files/Playlists`
- Destination: `/storage/.kodi/userdata/playlists/music`
- Uses mtime comparison for change detection
- Verified: Python syntax valid, repo generator zips it cleanly

## Files Changed
- `deploy.sh` — simplified to omega-only
- `omega/service.playlist.fixer/addon.xml` — new file
- `omega/service.playlist.fixer/service.py` — new file

## Key Decisions
- Simple polling pattern (like `service.tvtunes`) over event-driven approach
- mtime comparison over content hashing for change detection
- No settings UI — hardcoded paths since the device environment is fixed
- 60-second poll interval as a balance between responsiveness and resource usage

## Pending / Not Re-requested
- Now-playing info persistence was reverted and not re-requested
